{
  "name": "swarm-next",
  "private": true,
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "build": "pnpm --dir web build",
    "check": "pnpm --dir web check",
    "test": "pnpm --dir web test",
    "test:dogfood": "node --test scripts/dogfood/*.test.cjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
