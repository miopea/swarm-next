# User-facing maturity closeout — September 11, 2026

This is the current delivery handoff, not completion of the full program in45.
Detailed receipts remain in92. Older pending notes in45/92/93/95 are historical;
the newest checkpoint for the same gate supersedes them.

## Delivered and verified

| Outcome | Evidence and boundary |
| --- | --- |
| Join an Apiary without Jira | Recorded live Daisy journey: submit, one Keeper approval, server completes joining with browser closed; shared tasks reconcile with zero Jira setup. Demo departed cleanly. Do not repeat this closed journey. |
| Existing members keep membership | WSL remains joined; its name can be changed and restored through management without rejoining. Keeper and member retain three-Hive directory. Automatic propagation latency without explicit retry remains unmeasured. |
| Useful Apiary refresh |82eac78a: global refresh updates directory without page reload or discarding a management draft; live two-Hive acceptance passed. |
| Bounded management reads |42ace825: cancellation on replacement/unmount, eight-second deadline and retry; Keeper/member failure tests and live management smoke passed. Does not change write authority. |
| Everyday worker setup | Roots configuration, repository search, tilde path override with explicit consent, missing-folder error/retry with retained inputs, and new Sleeping state verified on WSL. Disposable worker removed; files retained. |
| Honest worker status |61e36a10 removes false API read-only-sandbox evidence; actual Architecture worker now Resting without permission/session changes. Real engine-owned read-only detection remains open. |
| Needs You recommendations |5439a9f8 supports explicit No preference without a fake choice or automatic answer; genuine recommendations stay visible. |
| Needs You interactions | Actual components in fictional Edge fixtures: custom text, failed-send retention, exact retry, clarification/reply attribution, correct attention counts and explicit final answer. This is not live/native terminal correlation. |
| Interview draft continuity |cd7889fa fixes reproduced custom-answer/note loss across Activity; changed questions still reset answers. Regression and390px interaction passed. |
| Worker setup draft continuity |09bc29cc preserves unfinished worker entries across Settings sections and filtering. Regression failed before the fix; Edge fixture and deployed WSL retained the exact name and repository. No worker was created by these draft-only checks. |
| Queue presentation | Live owner sections, worker grouping, recorded order, linked prerequisites and expandable explanations. Counts match37 waiting rows at the recorded checkpoint. This does not prove every underlying task is correctly orchestrated. |
| Narrow queue/decision interactions | September11 Edge390x844 fixtures: owner shortcut reached the blocked group; task navigation focused the correct card and opened readable details with reachable Close/Save. Expanded long risk text did not overlap choices; the final long option and Send answers remained reachable. No task or answer was submitted. This is browser layout/interaction acceptance, not native-device proof. |
| Paired support attachment recovery | September11 actual Swarm transport and Admin fixture routes: two tests passed for text/files, lost receipt, Hive reopen, exact replay, changed-content rejection and original-receipt preservation. Fictional memory-only Admin storage; no production activation or customer send. See86. |

## Exact current delivery

- Production dev and WSL App/API: `1.7.1-dev-09bc29cc0c54-20260911144639-1439784`.
- Source commit: `09bc29cc0c5480aead8f57b43f1363839829e8d2`; subsequent handoff/fixture changes do not change the shipped application.
- Engine package intentionally remains `1.7.1-dev-6d1ac295a1d4-20260911121244-1346703`.
- Production engine PID1364561; WSL engine PID45338. Both preserved during updates.
- Eight production worker/session pairs match before/after in `/tmp/swarm-settings-draft-continuity.rRcd1B`; both hashes are `6f5196dda450ba7055d488e5ad8522c3fce006b1ecf8d203bf243fa64f100ca9`.
- Previous full web:174 files/1684 tests passed. Current change:137 Settings, worker setup and App tests passed; TypeScript, production web and Linux development package builds passed.
- Priorcd7889fa full CI34610465314 succeeded. Current09bc29cc full CI34612113986 also succeeded: web, Rust audit, Linux packaging and Rust all passed. Do not repeat the completed gate.
- No release cut by this work. No worker-engine update applied. No BFG Admin communication.
- Worktree: `C:\projects\swarm-next\.worktrees\recovery-main-20260907`; branch `codex/apiary-and-workspace-ux`; changes pushed to main.
- Preserve unrelated untracked `scripts/dogfood/native-signal-probe.cjs`.

## Release assessment

The delivered onboarding/worker-setup/attention UI improvements are suitable for
focused developer dogfooding. Current CI is green; the incremental candidate is
ready for the release worker's review using [the post-1.8.0 handoff](post-1.8.0-release-handoff.md).
Do not market the full maturity program or broader Keeper management
as complete. Existing users should upgrade in place, not leave and rejoin.

Material exceptions to disclose:

1. Answering directly in a native terminal can still leave Needs You unresolved;
   authenticated provenance and exact native answer linkage remain incomplete.
2. Queen may still show external waits between review runs. A changed presentation
   requires the pending operator answer in09; do not silently reinterpret it.
3. The automatic engine-update/recovery reliability gate is not complete. Pending
   engine updates are separate from the verified App/API updates here.
4. Mobile camera/gallery, suspension/cutover and full narrow-screen interaction
   need real-device evidence. Desktop390px samples are not physical-phone proof.
5. Production disk has approximately3.6GiB free after removal of the verified
   inactive native-answer compiler cache. Inspect headroom and only verified
   inactive reproducible build artifacts before another large build; preserve
   sources, data, attachments, backups, installed packages and active processes.

## Remaining work, not hidden by the UI milestone

| Priority | Next bounded outcome | Completion evidence |
| --- | --- | --- |
| User-facing acceptance | Finish off-screen/narrow interactions and real device journeys | Actual Android/iOS camera/gallery, composer draft recovery, ownership cutover; preserve prior accepted AskUser2/3 and task-editor checks. |
| Attention correctness | Native answer provenance and audited exact decision linkage | Final human-origin answer, full IDs/question/options match, one resolution, uncertain/replayed/wrong-session negatives. Keep capture off until proven. |
| Orchestration | Remaining unknown/failed recovery and real backlog reconciliation | Queen owns a bounded recovery and escalates only a genuine operator need; no manual task shuffling to manufacture success. Retain completed missing-input/corrective demos. |
| Operations | Exact conversation recovery and safe engine admission | Chosen conversation persists; safe recovery, provider continue, clearly identified fresh fallback; cancellation/failure/partial-stop/update-return receipts. |
| Efficiency | Matched fresh versus aged workload | Browser and server attribution, output fidelity and sustained plateau. Payload reduction/short server baseline are not this gate. |
| Metrics | Measure useful progress | Review yield, duplicate questions, false alerts, recovery outcomes and update convergence, not just run counts. |
| Support | Production attachment activation and linked task/approved-reply loop | Paired route recovery now passes; native picking, production private storage and linked completion/approved reply still need end-to-end proof. Admin owns customer sends. No BFG worker contact without approval. |
| Apiary extension | Capability-specific Keeper management | Scope in98 still design: applied configuration receipts, revocation, offline convergence and bounded transport. Joining is not arbitrary remote-command permission. |

## Short operator dogfood checklist

- Invite a new developer using a fresh link. They submit once; Keeper approves
  once; Jira is optional and setup guidance stays in Apiary.
- Add a normal repository worker from search or an explicit local-machine path.
  Errors should preserve entries; a newly created worker should be Sleeping.
- On a genuine Needs You decision, ask for clarification or use Say something
  else. Do not confuse answering inside a terminal with the still-open native
  correlation feature.
- On your phone, report any inaccessible control, lost draft or attachment that
  never acknowledges selection. Native AskUser rendering and task-editor checks
  already accepted should only be reopened if they regress.

At roughly10% remaining, finish the current package and consolidate this handoff;
at roughly5%, stop new implementation. Leave the full goal open with explicit
remaining gates. Do not consume resets, contact another worker, or cut a release.

### Final budget-directed package checkpoint

Account reported10% remaining on September11 around10:52 Eastern. Protect5%.
The three delivered user-facing slices are no-Jira one-approval enrollment,
everyday worker setup, and Needs You custom answers/clarification/draft continuity.
Their recorded acceptance is complete within those boundaries; the full Apiary,
native answer reconciliation, orchestration and mobile programs are not complete.

The Settings browser fixture had an obsolete workspace string-array response;
typing a repository caused that fictional app to go blank. Updating the fixture
to the existing WorkspaceChoice contract restored the interaction. This was not
reproduced against live WSL. Exact draft retention passed there; the test draft
was cleared, and all testing tabs were closed. No credentials, membership,
worker assignment or persistent configuration were changed by this check.

### Budget checkpoint and isolated recovery cleanup

September11 around11:30 Eastern: live account meter reported8% remaining.
Preserve5%; no new investigation or repeated completed acceptance suite.
Use the remaining allowance only for a bounded existing gate or release handoff.

The isolated Swarm Dogfood Contract worker was awakened with no assigned tasks.
Its terminal reported restored conversation
`c4435eee-57b0-4546-b6ef-dc182080e7b5`. A native `/clear` and one fictional,
no-tools prompt created a second conversation and returned `resume-fixture-ready`.
Native `/resume c4435eee-57b0-4546-b6ef-dc182080e7b5` restored the original;
Session details confirmed that exact selection was saved for the next start.
The demo was then put back to sleep. Read-only verification confirmed
`running:false`, no active session, no background work and zero assigned tasks.
No project worker was restarted and no engine update was applied.

This is not acceptance of switching to the second conversation across sleep/wake,
nor of missing-context/continue/fresh fallback. Those remain open. The harmless
fictional second conversation `e38c03b3-c21b-437a-9e6a-2794d1887604` remains on
disk; freshness now labels the intentionally restored older demo conversation
stale. Do not treat that timestamp comparison as proof the chosen identity was
lost, and do not delete provider history just to make the diagnostic green.

### Departmental setup regression

Added a focused MemberSetup regression for the operator's IT-project example:
one ready project remains ready after an inaccessible department is added;
membership remains intact in the presentation, configuration is optional, and
the setup action disappears when the second project becomes ready. No implicit
management or refresh callback is invoked. All26 tests across MemberSetup,
SharedCatalogStatus and MemberControlRoom passed, as did TypeScript checking.
This is presentation-level failure/recovery evidence, not new proof of live Jira
permissions, assignment enforcement or cross-Hive propagation. No runtime code,
deployment or release was needed.
